# 生效决策表（用户拍板，勿再返工）

| # | 决策（当前生效） | 沿革（被取代的旧值） |
|---|---|---|
| 1 | 段间缓冲 0.1s + 成片静音验证 | 曾段尾 2.5s 缓冲静音被否（「中间的停顿，非常不爽」） |
| 2 | 语速 **speed 1.15 + pitch +2**（2026-08-10 试听定稿） | 曾 1.0x 原速（嫌慢）、1.2x（嫌快）、1.2/+5（太亮太急） |
| 3 | 尾卡四要素：品牌图 + 黄色「关注「数解AI」」+ 当期文章标题 +「查看公众号文章」引导 | 曾只有文字尾卡；曾缺当期标题 |
| 4 | 字幕：**75 号**、MarginV=210（品牌条上方）、**整行 + `{\fad(150,80)}` 淡入**、拆句阈值 26 | 曾 44 号→100 号→75 号；曾打字机逐字（「不要逐字」） |
| 5 | 内容占屏 ≥40%，内容最低点距底 **399~800px**（2026-08-16 起由 #47 整页规划统一保证） | 曾 501px 上限被否（字幕 1 行时内容可下探） |
| 6 | 布局原子化：VGroup + arrange + 锚点链 + 安全区，禁裸坐标（整页定位统一走 #47 的 layout_page） | 曾逐元素 move_to 魔法数字，换分辨率即越界重叠 |
| 7 | 框内文字限宽 0.72~0.85（含嵌套框），`boxed()` 工具 | 曾 6 处溢出事故（Transformer/WordPiece/9 字中文） |
| 8 | 弧线：侧边/下方起终点、扫过区禁元素 | 曾贴顶「帽子」弧、穿越下方内容被否 |
| 9 | ≥5 节点链路蛇形两行；禁止假占位符 | 曾单行硬塞每框 1.47 单位；曾 [—] 凑数被否（「非常丑陋」） |
| 10 | FadeIn shift ≤0.1；验证抽动画中帧 | 曾 0.2 位移暂停逐帧看到错位 |
| 11 | **整段配音 + 段尾过渡句 + 时间戳切分**；默认 speech-2.8-turbo | 曾逐段 TTS（段间话题跳跃生硬）；曾 hd（贵） |
| 12 | 时长门禁：生成前 `wc -m` 预估（×0.19~0.20）；默认 ≤4:20；**本篇用户拍板可放到 5:30** | 曾生成后报超长反复返工；Transformer 2026-08-13 拍板 5:30 |
| 13 | 动画节奏 = 台词时间轴；`pad_to_voice()` 兜底，静止 ≤20% | 曾动作挤前 1/3 后 2/3 画面僵住（FP4 复盘） |
| 14 | 转折前停顿用 `<#0.5#>`；拟声标签整段生成可用 | 曾 `(inhale)` 被否（只管发声不管停留） |
| 15 | 封面关键内容集中在**中间 3:4 安全区**（y∈[240,1680]）；成图用 yairouter | 曾全幅布局被 3:4 裁切；曾用 Manim Cover 当发布封面 |
| 16 | 完播率三件套：**开场 3 秒钩子 + 段间悬念钩 + 结尾预告&互动** | 曾平铺直叙，完播率低（2026-08-11） |
| 17 | 否定/纠错视觉：**`play_red_cross()` 动态大红叉**（两笔 GrowFromCenter + 弹跳）+ **白色文字**（2026-08-11 注意力视频 3 处） | 曾红字 + 单条斜线被否（「文字就不要用红色了」「红叉叉最好加一个动态效果」） |
| 18 | **√d 用模板内置 `sqrt_group()`**（√ 字形自带横线只到字形右缘，手绘横线从右缘向右延伸、细线）；系统无 LaTeX 时禁用 MathTex | 曾 Text 直写 √d（3 处不连贯）→ 曾纯线条 3 版（像 \\d/¬/v+d）→ 曾横线叠字形上（粗杠，2026-08-11） |
| 19 | **boxed()/fit() 只缩小不放大**（`if width > limit` 才 set_width） | 曾无条件 set_width 放大短字符（Q/K/V/追/猫/qᵢ 顶出框，2026-08-11 四连发） |
| 20 | **方框入场默认拉幕** `play_scroll_unroll()`（全高、从左向右摊开，多框等宽） | 曾 `FadeIn` 整框弹出（吸引力弱，2026-08-14 预训练 S6 改拉幕后拍板） |
| 21 | 箭头与框留缝、两端居中、换页带走 | 曾贴地/贴框残留数秒（归一化 00:29）、竖箭头不居中（Transformer 04:00） |
| 22 | 屏幕小字说明不改音轨；台词有公式则画面出公式 | 曾只改字幕没画面（归一化 00:54）、缺「以 XX 为例」（三片都补） |
| 23 | 手绘坐标图：先定 origin；柱离轴；趋势过顶；标签不撞刻度 | 曾柱陷轴下、xlab 落画布外、直线只连首尾（预训练 S4） |
| 24 | 外框包住内框；代码框贴合行数居中；下面有空别挤 | 曾代码框过大（归一化 02:53）、混合框没包住 4 个 Norm（03:26） |
| 25 | 转折/爆点只留关键词 + 一次闪耀 | 曾 ⚠+Indicate+Flash 叠三层、「可它。没崩。」复述整句（预训练） |
| 26 | **配音方式门禁**：Step 2 问清 TTS 克隆 / 口播，全程一致不中途切换；口播用 `scripts/voice_process.py` 修音+时长+停顿分析（2026-08-16 新增） | 曾只有 TTS 克隆单一配音路径 |
| 27 | **口播字幕停顿对齐**：`tts/pauses.json`（真人停顿边界）驱动 build 字幕，优先级高于 TTS 句子时间戳 | 曾只有字数比例 / TTS 时间戳两种字幕策略 |
| 28 | **口播录音引导**：一段一段录+逐段核对，句间自然停顿 0.3-0.5s，语音占比 <15% 重录 | 曾一次性塞全部段落无核对 |
| 29 | **段间一致性补偿**：波形(`astats` RMS)+频谱(`bandpass` 分 4 频段测 RMS) 分析 → 选频谱最均衡段为锚 → 各频段 EQ 对齐（>3dB 才补、≤6dB 防失真）→ loudnorm 统一。`--no-consistency` 关闭；单频段触顶补不回 → 重录（2026-08-16 新增） | 曾只逐段 loudnorm 统一响度，不解决频段能量差异（发虚/发闷） |
| 30 | **口播录音用 ffmpeg**：ffmpeg -f alsa -i hw:<card>,0 -t <秒> -ar 48000 -ac 2 -y recordings/sN.wav；录音前 arecord -l + volumedetect 探测有信号的麦克风（CM40 USB 麦 48kHz 立体声 / 内置麦 44.1kHz 单声道）；录音窗口 = 预估时长 + 10-15s 余量，避免最后一句被截断（2026-08-15 RLHF） | 曾手敲 ffmpeg 命令、用错麦克风、窗口不够截断最后一句 |
| 31 | **口播录音裁剪静音**：录音后自动裁剪头尾静音（-ss start -to end -c copy）；开头微弱环境声会被误判为语音起点（S6 2.86s 静音残留），用第一个明显语音段；尾部静音残留（S2 6.3s），裁剪终点用最后一个语音结束点（2026-08-15） | 曾裁剪后残留段内长静音，成片 2:22 处 2.86s 静音 |
| 32 | **字幕按完整句子出现**：句号/问号/感叹号/分号切分，不拆半句；长句在逗号处拆成语义片段（用户认可逗号分割）；每条 ≤26 字（最多折 2 行，不要 3 行）；英文单词不拆断（中英文混杂在空格/单词边界断行）；整条一次出现（不用逐字打字机）；fad(60,40) 避免字被吞（2026-08-15） | 曾字幕拆半句、折 3 行、英文拆断、逐字滚动、字被吞 |
| 33 | **字幕音画同步**：字幕时间戳按字数比例分配会导致和画面切换不同步（字幕比画面早 2-3s）；用 tts/pauses.json 停顿点驱动字幕切分——**每个真实停顿都是字幕边界**，文本按字符比例分配到停顿槽、在自然标点处对齐（0 个停顿被跳过）。旧算法「先按标点切文本再均匀采样停顿点」会跳过 28% 真实停顿（RLHF 96 个停顿跳 27 个），导致字幕跨过说话者自然停顿→不同步（2026-08-17 重写）。**pauses.json 语义 = 静音结束点**（下一句语音起点，非中点；开头 <0.3s 静音过滤） | 曾字幕比画面早 2-3s，02:06 起不同步；曾均匀采样跳过 28% 停顿（00:44 跨过停顿 5.4s 字幕不切） |
| 34 | **成片自动验证**：验证字幕折行（≤2 行）、无英文拆断、无左右截断、音画同步、无长静音（<1.5s），避免用户看样片才发现问题（2026-08-15） | 曾用户看样片才发现字幕截断/不同步/静音 |
| 35 | **停顿检测阈值 -30dB**（2026-08-17）：voice_process.py `SILENCE_DB = -30`（曾 -35）。-35dB 漏检 8% 真实停顿（RLHF 96→104，+8 个），如 02:06 处 0.37s 停顿未检出→字幕「压成 0 到 1 的概率」跨过停顿 3.65s 不切。-30dB 从 trim/ 目录取停顿中点（非 recordings/ 原始录音，避免未裁剪时间轴偏移） | 曾 -35dB 漏检 02:06 停顿，字幕跨过说话者自然停顿 |
| 36 | **scene 代码模块化（2026-08-17）**：通用工具全部收敛到 `scripts/manim_helpers.py`（config/颜色/t/_card/boxed/fit/sup/sub/type_in/cnode/arc_curve/_Base 全家桶），新文章用 `scripts/manim_scene_template.py`（自动向上查找 scripts/），scenes.py 只留 VOICE_DUR/TAIL/背景色/场景类 | 曾每篇复制粘贴工具函数（13 篇 scenes.py 各自演化，v7 修 bug 要改全篇副本） |
| 37 | **裸文字打字机 + 慢速（2026-08-17 拍板）**：type_in 标题 1.1s/正文 0.8-1.0s/标签 0.5s；拉幕 ×1.5 慢速（q 1.5、三卡 1.2、ans 1.0） | 曾文字一次性 FadeIn 整段（用户否）；曾打字过快（「文字出现速度不能太快」） |
| 38 | **闭环弧线贝塞尔不穿圆（2026-08-17 拍板）**：arc_curve() 端点圆外 1.15R + 控制点外法向 + 箭头尖贴圆周；像素验证弧线色像素到圆心距离 ≥R | 曾 CurvedArrow 弧线穿圆（pg 圆内 3009 像素，用户报「箭头和圆圈交叉」v7→v8） |
| 39 | **FadeOut 对账含段内换页（2026-08-17 S7 事故）**：段2→段3 换页也必须带走前一段全部元素（lab 残影叠压 notfixed/shortcuts 5s；shortcuts 叠压 b_lab） | 曾只对账场景级换页，段内换页漏元素残留 |
| 40 | **质量检查 subagent（2026-08-17）**：渲染后 + 构建后必须派 `.pi/agents/manim-qa-reviewer` 跑 `docs/qa-checklist.md`，全 PASS 才能宣称完成 | 曾靠主 agent 自查，用户时间轴反馈大多是漏查（S7 残影 5s、弧线穿圆） |
| 41 | **先声音后动画（2026-08-17 用户拍板）**：配音/录音完成并确认前禁止设计动画（at() 排布、出现/滞留时间）；元素出现时间 = 字幕起始戳、滞留时间 = 字幕结束/下一句开始，与切分字幕时间线完全对应 | 曾按预估时长设计动画 → 实录/生成时长变化 → 音画错位反复返工 |
| 42 | **生动化三件套（2026-08-15 沉淀）**：AI 概念图嵌入（yairouter+make_round_logo，**禁数字/年份/公式**）+ 数据动效（counter_value/grow_bar/轨迹 Create，数字台词必配动效禁纯文字）+ 场景转场（transition_out）。emoji 不可用（Manim 0.21 丢彩色字形） | PPO 复盘：S6/S8/S9 图形 0、全片零插图、动画仅 type_in/scroll_unroll/FadeIn 三件套，单调 |
| 43 | **多 agent 并行写 scenes（2026-08-15 用户拍板）**：≥6 场景新写时，**≤4 并行 writer 只写不渲染**、合并后渲染、reviewer 只读不改、修复按全局/局部分发、2 轮上限防震荡 | 单 agent 串行写 9 场景，布局靠脑内估算、QA 20 分钟一轮反馈慢 |
| 44 | **manim 0.21 DecimalNumber 用 Text（2026-08-15）**：v0.21 起 DecimalNumber 默认 mob_class=MathTex（需 latex）。本机已装 texlive（latex 可用，MathTex 可渲染复杂公式），但 counter_value 仍显式 mob_class=Text 规避依赖（数字用 Text 足够）；升级 manim 先单场景 -ql 冒烟 | helper_demo 渲染 `FileNotFoundError: 'latex'`，查为 0.21 升级改默认渲染类；后装 texlive 解决 |
| 45 | **数字对比先旧后新（2026-08-16 GRPO 00:49 用户反馈）**：台词念「从 A 冲到 B」时，画面必须先出 A（旧值淡化标签），再 counter 滚动 A→B；禁止滚动完才补旧值标签 | 曾 counter 滚动完才 type_in 旧值（顺序反，用户否） |
| 46 | **内容下移利用字幕上方空间（2026-08-16 GRPO 用户反馈「内容的编排都太靠上面了」）**：内容最低点距底 399-800px，优先贴近 399-600px；字幕上方空间（距底 399-800px 带）应被内容利用，禁止内容挤上半屏、下半屏空置 | 曾多页内容最低点距底 >800px（画面空） |
| 47 | **整页规划（2026-08-16 GRPO 用户拍板）**：每次换页都按该页全部元素的最终稳定状态组装整页 box → `page_stack()` 定页内相对位置 → `layout_page()` 把整页 box 居中放入显示带（`PAGE_TOP=0.32FH` 到 `PAGE_BOTTOM=-0.292FH`）；**上下留白严格相等，且各 ≤ 显示带 30%（内容高度 ≥40%，helper 直接 ValueError）**；短页用放大元素/加大页内间距满足下限，闪烁/强调类装饰（红叉/circumscribe/indicate/breathe/数字滚动）不参与整页 box。禁止 `next_to(head, DOWN)` 接龙式排布。工具已固化进 `scripts/manim_helpers.py` 的 `layout_page`/`page_stack` + 模板 | 曾 VGroup+锚点链从标题向下堆（页挤上半屏）；曾底边对齐（短页顶部留白过大）；曾整页居中但不强制 40%（短页留白 >30%） |
| 49 | **卡片默认实心 + 轻微圆角（2026-08-16 GRPO 用户拍板）**：所有文本方框统一 `_card()`/`boxed()`：实心填充 `CARD_FILL=#2C3F60`（中性石板蓝默认色，不与黄/青/绿/红高亮混淆）、`fill_opacity=1.0`、`RoundedRectangle(corner_radius=0.18)`；`play_scroll_unroll()` 同步圆角拉幕。普通 `Rectangle` 只用于柱状条/数据块/装饰线，不再用于文本卡片 | 曾 `Rectangle` + 半透明填充，方框生硬、默认色与高亮色容易撞 |
| 48 | **字幕时间戳优先 `tts/sentence-boundaries.json`（2026-08-16 GRPO 用户反馈字幕不同步）**：build 脚本自动优先级 = Web `manual-boundaries.json` > `sentence-boundaries.json` 逐句 start/end > `pauses.json` 停顿兜底 > `full.subtitle.json` > 字数比例。`sentence-boundaries.json` 的 clips 文本必须逐字拼回 tts.txt，start/end 与 trim 后语音逐句对应；build 时按实际配音时长等比缩放。split_long 不得拆断英文/数字串（DeepSeekMath/77.9%/2024） | 曾只认 `pauses.json`，用停顿槽按字数比例重分文本 → S2「GRPO 的起点」挂 6.1s、后续整句错位；曾 split_long 把 DeepSeekMath/77.9% 切成两半 |
