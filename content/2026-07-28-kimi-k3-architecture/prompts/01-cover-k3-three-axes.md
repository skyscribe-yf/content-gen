---
illustration_id: 01
type: infographic
style: notion
palette: warm
aspect: "21:9"
---

## ZONES

### Zone 1 — Central Visual
A giant translucent brain at center, with three glowing axes radiating outward like a 3D coordinate system:
- X-axis (horizontal, warm orange): labeled "Sequence" with a scrolling tape of tokens flowing left to right, getting compressed into a small fixed-size state box at the end
- Y-axis (vertical, warm teal): labeled "Depth" with 93 tiny layer blocks stacked, and dotted retrieval lines jumping from top layers back to early layers
- Z-axis (diagonal, warm purple): labeled "Channel" with 896 small expert nodes, only 16 of them glowing bright, connected to a central routing hub

### Zone 2 — Title Area (top)
Large clean title text: "Kimi K3"
Subtitle: "Three Axes, 2.8T Parameters"

### Zone 3 — Bottom Caption
Three short labels under each axis:
- "KDA: O(T²) → O(T)"
- "AttnRes: O(Ld) → O(Nd)"
- "Latent MoE: 896 → 16 active"

## LABELS
- Title: "Kimi K3"
- Subtitle: "Three Axes, 2.8T Parameters"
- Axis labels: "Sequence", "Depth", "Channel"
- Bottom formulas: "KDA: O(T²) → O(T)", "AttnRes: O(Ld) → O(Nd)", "Latent MoE: 896 → 16 active"

## COLORS
- Primary: #E8756D (warm coral)
- Secondary: #7ECFC0 (warm teal)
- Accent: #B8A4D4 (warm purple)
- Background: #FFF8F5 (warm white)

## STYLE
Clean composition with generous white space. Simple gradient background from warm white to subtle cream. Main elements centered. No complex backgrounds. Notion-style flat illustration with subtle shadows. Color values (#hex) and color names are rendering guidance only — do NOT display color names, hex codes, or palette labels as visible text in the image.

## ASPECT
21:9 cinematic widescreen, suitable for article cover
