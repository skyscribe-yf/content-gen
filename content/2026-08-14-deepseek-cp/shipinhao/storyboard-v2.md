# 《上下文并行：1M序列为什么切了会坏？》视频号分镜脚本 v2

基于 content/2026-08-14-deepseek-cp/weixin.md
规格：竖屏 1080×1920 · Manim 动画 · MiniMax 精英男声 TTS（speech-2.8-turbo，speed 1.0 pitch +2；S6 结尾降速 0.85）+ 烧录字幕 · 目标 3-4 分钟

## 段落设计（6 段，2026-09-03 重做：原 8 段压缩为 6 段，动画全部挂 sentence-boundaries 时间戳）

| # | 场景 | 动画要点（每页 1 个主视觉动效） | 配音稿 |
|---|------|---------|--------|
| S1 | 开场钩子：切蛋糕 → 一切就坏 | 概念图（切蛋糕）→ 8 段序列条 → 转折爆点 | 1M token 的序列，切成 8 段，分给 8 张 GPU——听起来像切蛋糕，每人一块，有什么难的？DeepSeek-V4 的回答是：一切就坏。今天讲清楚，为什么切了会坏，两阶段通信怎么修好。 |
| S2 | 为什么必须切：1.3TB 显存账 | 21.5GB/层 → 61 层 → 1.3TB 数字滚动 → 16 倍对比 → Flash Attention 只压分数矩阵 | 先看为什么要切。1M 序列，单层注意力要留住 Q、K、V 三份张量，约 21.5GB；V4 有 61 层，激活总量 1.3TB。一块 H800 只有 80GB——差了 16 倍。你可能会问：Flash Attention 不是解决了吗？它只压掉了分数矩阵，Q、K、V 一分没少。所以 CP 不是优化，是必须。1M 上下文训练，没有 CP 根本跑不起来。 |
| S3 | 普通 CP 两个假设 + Ring-Attention | 两个假设卡片逐条亮起 → 击鼓传花概念图 → 转折爆点（同时打破） | 普通 CP 怎么切？序列沿长度切段，每张卡持有一段连续 token，各算各的注意力。它成立，靠两个隐含假设：第一，本地 token 数和本地 KV 数大致一一对应；第二，边界好处理，跨段补一补就行。早期方案 Ring-Attention，就是把 KV 块像击鼓传花一样轮流传着算。但 V4 的压缩注意力，把这两个假设同时打破了。怎么破的？ |
| S4 | 两个坏因：长度不齐 + 窗口跨边界 | 坏因一：packed 序列示意（1000+7，尾部丢弃）→ 坏因二：跨边界块示意 + 照片相册概念图 | 坏因一：压缩后 KV 长度不齐。训练样本是多个序列打包的，每个序列按自己的边界独立压缩，尾部不足的 token 直接丢弃。比如一条 1000 个 token，一条只有 7 个——7 个凑不出 2 个完整块，尾部 3 个被丢掉。每个 rank 产出的压缩 KV 数量彼此不等，all-gather 第一步就卡住。坏因二：压缩窗口跨边界。压缩要 m 个连续的 KV entry，如果恰好有 m 个 token 横跨两张卡的分界线，左边只有前半块，右边只有后半块——谁都没法压。就像照片 4 张一组，相册切给两个人，边界切在两张照片中间。这组照片，丢了。 |
| S5 | 两阶段修复：原料交换 + select-and-pad | 阶段一：rank r → r+1 原料箭头流程 → 阶段二：pad → all-gather → blob 有洞 → select-and-pad 三步合一（C0/C1/C2/C3 例子） | V4 的修法分两步。第一步，交换边界原料：每个 rank 把自己末尾最后 m 个未压缩的 KV 发给右邻居，右邻居拼成完整块，在本地压出来。注意，传的是原料，不是半成品——跨边界块在左边根本不是一个完整块。这一步通信量与序列长度无关，CSA 层只要约 2KB，几乎可以忽略。第二步，all-gather 加 select-and-pad。边界块补齐了，长度不齐还在。每张卡先把产出 pad 到统一上界，再 all-gather。但 gather 出来的数据里有洞，padding 会污染注意力。所以最后一步 select-and-pad：去 padding、尾对齐、稀疏重排，合并成一个 kernel，把 padding 全部滤掉。那这笔账，到底省了多少？ |
| S6 | 账 + 实验 + 总结 + 尾卡 | 9MB vs 72MB 对比条 → 实验 277/276/277 对比 → 总结句 → 预告 + 问题 → 品牌尾卡（驻屏 ≥1.5s） | 算笔账：压缩后每层平均约 9MB 通信，在 NVLink 上只要 0.02 毫秒；不压缩直接 gather，是 72MB——省了 8 倍。模拟器跑出来：朴素 CP 缺了跨边界的压缩块，两阶段 CP 与单卡基准完全一致。所以，注意力公式变了，并行策略不能原封不动。CSA 不只是推理更快、KV 更小，它连 rank 间怎么通信、张量怎么对齐，全都改了。下一篇，拆 DualPipe 和 DeepEP：训练时 GPU 到底在等什么。一个问题留给你：压缩省通信和流水线藏通信，你更看好哪条路？评论区聊聊。关注数解AI。 |

## 爆点节奏（4 种轮换）

- S1 问句钩子（有什么难的？）+ 转折爆点（一切就坏）
- S2 数字对比（16 倍）+ 转折（Flash Attention 只压分数矩阵）
- S3 问句（怎么切？）+ 转折（两个假设同时打破）+ 悬念（怎么破的？）
- S4 短句断句（这组照片，丢了）+ 比喻（相册切两半）
- S5 转折（传原料不是半成品）+ 转折（gather 数据里有洞）+ 悬念（省了多少？）
- S6 数字对比（9MB vs 72MB，8 倍）+ 总结 + 互动（你更看好哪条路？）

## 段间悬念钩

- S1 尾「为什么切了会坏」→ S2 揭晓「先看为什么要切」
- S2 尾「没有 CP 根本跑不起来」→ S3 揭晓「普通 CP 怎么切」
- S3 尾「怎么破的？」→ S4 揭晓「坏因一」
- S4 尾「这组照片，丢了」→ S5 揭晓「V4 的修法分两步」
- S5 尾「这笔账省了多少？」→ S6 揭晓「算笔账」

## 插图位规划（3 张概念图，yairouter 深蓝风，无文字）

| 图 | 场景 | 比喻 | 用途 |
|---|---|---|---|
| s1-cake-round.png | S1 | 蛋糕切成 8 块分给 8 人（1M 序列切 8 段） | 开场钩子配图 |
| s3-relay-round.png | S3 | 击鼓传花（KV 块轮流传着算） | Ring-Attention 配图 |
| s4-album-round.png | S4 | 相册切两半，边界切在两张照片中间 | 压缩窗口跨边界配图 |

## 制作流程

1. 配音：`scripts/minimax_tts.py` 整段生成（精英男声 male-qn-jingying，speed 1.0 pitch +2）→ S6 单独 speed 0.85 重生成拼接 → `tts_split.py` 切分 6 段
2. 概念图：`scripts/yairouter_img.py` 3 张 → `make_round_logo.py` 圆角
3. 动画：每段一个 Manim Scene（S1..S6），`at_clip` 挂 sentence-boundaries.json，`construct` 末尾 `pad_to_voice()`
4. 构建：`scripts/manim_video_build.py` mux 配音 + 无缝拼接 + 黄色字幕烧录 + 品牌尾卡
5. 归档：`content/2026-08-14-deepseek-cp/shipinhao/`
