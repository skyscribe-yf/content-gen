# 文章质量核查：归一化与残差

核查日期：2026-07-18

## 已通过

1. **数据准确性**：已拉取并核验 DeepSeek-V4-Pro 固定提交 `b5968e9190ef611bbf34a7229255be88a0e937c1` 的 `config.json`：`num_hidden_layers` 为 61、`hidden_size` 为 7168、`rms_norm_eps` 为 1e−6、`hc_mult` 为 4。正文链接到该配置及官方 `inference/model.py`。
2. **逻辑一致性**：明确区分恒等捷径的范数保持与 **x + F(x)** 整体范数仍可变化；明确 Pre-Norm 控制的是子层输入尺度，不宣称残差或 Pre-Norm 单独保证所有数值稳定性。
3. **本土化**：实践案例使用 DeepSeek-V4-Pro，不使用境外产品作读者操作示例。
4. **配图**：封面 1 张、文内图 5 张，均在文章根目录，且图片中文字与正文术语、数字一致。
5. **结尾钩子**：以“完全不动的原件通道”与“可学习多状态混合”之间的选择提出开放问题。
6. **系列回引**：开头内联链接到已发布的残差连接文章；尾部仅使用两个微信合集链接。
7. **公式格式**：已检查，无 LaTeX 分隔符；独立公式均使用 Unicode、引用块和加粗。
8. **图片路径**：`weixin.md` 的 6 个图片引用均无 `images/` 前缀，并与文章同级。

## 发布时人工完成

- 在公众号后台勾选原创声明并加入「大模型原理」合集。
- 正式发布后立即把实际微信 URL 写回 `weixin.md` frontmatter 的 `wechatUrl` 与 `draft-status.yaml`。
