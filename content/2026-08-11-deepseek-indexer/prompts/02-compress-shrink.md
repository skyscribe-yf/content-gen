---
type: infographic
slug: compress-shrink
backend: yairouter
model: gpt-image-2
size: "1:1"
quality: high
language: zh
output: "../02-compress-shrink.png"
references: []
---

# FULL PROMPT

为微信公众号技术文章生成正方形配图，1K 分辨率。主题：CSA 压缩把 128K token 压成 32K 个块（每 4 个 token 合成 1 块），解决了「存得下」，但没解决「算得完」。画面理性清晰，不使用人物。

## ZONES

- 左侧：密集的 16 个小圆点排成 4×4 网格（表示原始 token），每个小圆点标一个极小的浅色圆点纹理。
- 中央：一个「压缩漏斗」图标，四束小圆点从左侧流入，漏斗下方只流出 4 个较大的方块（表示每 4 个 token 压成 1 个块）。
- 右侧：4 个大方块上下排列（表示压缩后的块），其中一块微微发光（表示这 4 块只是示例）。
- 底部：一行极简的标语分隔线。

## LABELS

- 左侧小圆点组上方标注：“128K token”
- 右侧大方块组上方标注：“32K 个块”
- 漏斗下方标注一行小字：“每 4 个 token 合成 1 块（m=4）”
- 底部标注一行：“存得下 ≠ 算得完”
- 其余画面不出现任何文字、数字、英文、Logo。

## COLORS

- 主色：暖石墨灰（方块与圆点）＋ 琥珀橙（压缩漏斗与发光块，突出“压缩”动作）
- 背景：米白/浅暖灰渐变。
- 文字：深炭灰。

## STYLE

- 扁平矢量信息图风格，几何构成干净。
- 漏斗与方块有轻量立体感（细微渐变），圆点保持纯平面。

## ASPECT

- 正方形 1:1 构图，三组元素横向排布（左→中→右），底部标语留白充足。
