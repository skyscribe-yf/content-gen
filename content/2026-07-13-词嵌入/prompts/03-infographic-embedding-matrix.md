---
illustration_id: 03
type: infographic
style: sketch-notes
palette: macaron
---

嵌入矩阵：查表操作 - 数据结构可视化

Single-page hand-drawn educational infographic in a clean presentation style.
Warm cream paper background, black hand-drawn lines with slight wobble, soft
pastel color blocks. Feels simple, friendly, and easy to understand at a glance.
Diagram-style visuals ONLY — no realistic or photographic images.

PALETTE: macaron — soft pastel blocks on warm cream
COLORS: Warm Cream background (#F5F0E8); Black (#1A1A1A) for ALL lines, text,
        arrows, and doodles; section fills in Light Blue (#A8D8EA), Mint Green
        (#B5E5CF), Lavender (#D5C6E0), Peach (#FFD5C2); Coral Red (#E8655A)
        sparingly for emphasis points only.

LAYOUT (top → bottom):
- TOP: Bold hand-lettered title "嵌入矩阵 = 超级查表", oversized, slightly wobbly.
- MIDDLE: A large hand-drawn grid/table representing the embedding matrix:
          - Left column: token names (hand-lettered) — "今天", "天气", "真好", "，", ...
          - Top row: dimension indices — d₁, d₂, d₃, ..., d₇₁₆₈
          - Grid cells filled with small dots/numbers (suggesting "many values")
          - One row highlighted with Mint Green fill (the "猫" row, ID=2)
          - A one-hot vector [0, 1, 0, 0, ...] on the LEFT side, with the "1"
            position aligned to the highlighted row
          - A hand-drawn arrow from the one-hot × matrix operation pointing to
            the extracted row vector on the RIGHT side
          - Annotation: "one-hot × W = 抽出那一行"
- BOTTOM-LEFT: A small info box (Light Blue fill):
               "129,280 × 7,168 ≈ 9.27亿参数"
- BOTTOM-RIGHT: A small info box (Peach fill):
                "初始化: σ = 0.02 (随机)"
                "训练后: 反向传播自动调整"
- Bottom line: "本质就是一张 vocab × dim 的大表"

ELEMENTS: Hand-drawn grid/table, one-hot vector, multiplication arrow,
          highlighted row, info boxes with key numbers, doodle decorations.

STYLE: Minimal, well-organized, airy. Color fills don't completely fill outlines
       (slight "hand-painted" overshoot). ALL text hand-lettered — no computer fonts.
       Short labels and keywords only. Generous white space.

Color values (#hex) and color names are rendering guidance only — do NOT display color names, hex codes, or palette labels as visible text in the image.

Clean composition with generous white space. Simple or no background. Main elements centered or positioned by content needs.

ASPECT: 16:9
