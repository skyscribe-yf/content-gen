# 《SFT微调：1万条数据就能让模型听话？》视频号分镜脚本

基于 content/2026-07-29-SFT/weixin.md
规格：竖屏 1080×1920 · Manim 动画 · MiniMax 克隆音色配音（speed 1.15 pitch +2，speech-2.8-turbo）+ 烧录字幕 · 目标 ≤4:20 门禁
风格参考：content/2026-07-27-预训练/shipinhao/（scenes.py 工具函数同源）+ content/2026-07-18-注意力机制/shipinhao/

## 段落设计（8 段）

| # | 场景 | 动画要点 | 配音稿 | 爆点 |
|---|------|---------|--------|------|
| S1 | 开场钩子（成本矛盾：贵到独角兽放弃） | 大问号「预训练有多贵？」+ 36氪「六小虎」徽章（至少2家放弃）+ 李开复「ROI极低」卡 + Character.AI「2024.08 放弃」卡；矛盾箭头 →「必须从零造轮子吗？」 | 预训练有多贵？贵到连独角兽都撑不住。中国 6 家大模型独角兽，至少 2 家已经放弃了预训练。李开复说：超大基模预训练，对初创公司性价比极低。连硅谷的 Character.AI，2024 年 8 月也放弃了自研。那问题来了：每个公司，都得从零造轮子吗？ | 开场问句钩子 + 数字对比（6家→2家）+ 段尾悬念 |
| S2 | 账本对比：144 万倍 | 拿 Qwen3-8B 算账：82亿参数 + 36万亿token → 预训练 FLOPs（1.77×10²⁴）；1万条×2500token → SFT FLOPs（1.23×10¹⁸）；「144 万倍」爆点放大 + 人工成本 1.6 万卡 | 答案是不用。多数公司，只需在现成模型上做 SFT。拿 Qwen3-8B 算笔账：它 82 亿参数，预训练吃掉 36 万亿 token。算下来，预训练要 1 点 77 乘 10 的 24 次方 FLOPs。而 SFT，一万条指令回答对，每条平均 2500 token——总训练量 2500 万 token，只要 1 点 23 乘 10 的 18 次方 FLOPs。两个数字差多少？144 万倍。同样的参数，SFT 的成本，只有预训练的百万分之一。 | 数字对比爆点（144万倍）+ 数字阶梯 |
| S3 | 同一模型，两种命运 | 同一个问题「请解释什么是梯度下降」；base 框续写「算法，并提供一个简单的 Python 实现」+ 灰「方向偏了」；post 框「梯度下降是…迭代算法」+ 绿「定义/直觉/举例」；「参数相同」标签 vs 输出不同；段尾「后训练改了什么？」 | 同一个模型，同一个问题，两种命运。给 Qwen3-8B 的 base 版输入：请解释什么是梯度下降。它续写——算法，并提供一个简单的 Python 实现。它知道相关知识，却没认出这是个解释请求。而 post 版，同一个输入，稳定输出：梯度下降是一种迭代算法，定义、直觉、举例，结构清晰。参数规模相同，输出却完全不一样。问题来了：后训练，到底改了什么？ | 对比反转爆点 + 段尾悬念 |
| S4 | 实验：分布手掌→拳头 | RTX 4090 卡片 + base vs post；首 token 概率条：base「算法 0.570/法 0.209」宽而平 vs post「梯 0.813」窄而尖；手掌图→拳头图；五类指令聚合：改写 0.331→1.000 / 代码 0.082→0.846 / 情感 0.681→1.000；平均 top-1 0.820→0.916（+12%）、熵 0.896→0.333（−63%） | 我在一张 4090 上做了对比：同一个模型，base 和 post，五类指令。看首个 token。base 对'解释梯度下降'，把 57% 押在'算法'，21% 押在'法'，剩下撒向四面八方——像摊开的手掌。而 post，81% 都押在一个'梯'字上，直接以'梯度下降是'开头——像握紧的拳头。五类指令平均：top-1 从 82% 提到 91.6%，熵从 0.9 降到 0.33 bits。不确定性，少了近三分之二。 | 比喻爆点（手掌/拳头）+ 数字对比（−63%） |
| S5 | 入职培训比喻 | 预训练=「大学四年」卡（微积分/概率/编程）；老板问增长策略→「讲马尔可夫链」小插曲；SFT=「入职培训」卡（老板问策略就讲策略 / 用尊敬的别用亲爱的）；结语「改变的不是知识，是说什么、怎么说」 | 怎么理解这个变化？一句话：SFT 是入职培训。预训练像大学四年——微积分、概率、编程，什么都懂一点。但毕业第一天，老板问增长策略，你可能讲起马尔可夫链。不是不懂，是不知道这个场景该说什么。入职培训不教你微积分，它教你：老板问策略，就讲策略；写邮件，用尊敬的，别用亲爱的。SFT 改变的不是知识，是'该说什么、怎么说'。 | 比喻爆点 + 段尾对仗 |
| S6 | 数学：交叉熵收窄 | 交叉熵公式 H(p,q)=−Σp log q；惩罚力度表（0.50→1bit / 0.10→3.322 / 0.01→6.644 bits）；「概率越低罚越狠」强调；梯度公式 ∂H/∂z_j=q_j−1[j=x*] → 正确token负梯度上推、其他下压 | 数学上，这是把宽分布收成窄分布。SFT 用的还是交叉熵，和预训练一样。关键是数据变了：指令回答对。交叉熵的惩罚有多狠？模型给正确 token 概率 50%，惩罚 1 bit；降到 10%，惩罚涨到 3.3 bits；再降一个数量级，惩罚 6.6 bits。概率越低，罚得越狠——逼着模型把概率往正确 token 上集中。梯度也很直接：正确 token 的梯度是负的，把它的 logit 往上推；其他 token，往下压。收窄，就这么发生了。 | 数字对比爆点（0.01→6.6 bits）+ 转折 |
| S7 | chat template 考勤卡 | base 看纯文本「请解释梯度下降」；post 看被 <\|im_start\|> 包裹的 user/assistant/system 结构；角色标记点亮；考勤卡比喻卡；结语「告诉模型什么时候该说话」 | 还有个被忽略的关键——chat template。base 模型看到'请解释梯度下降'，只是纯文本续写。post 模型看到的，是被特殊标记包裹的：用户、助手、系统。这些标记告诉模型：现在轮到你回答了。它像入职培训的考勤卡——不教知识，只告诉模型，什么时候该说话。角色标记一进来，'接下来该输出什么'的概率判断，整个变了。 | 比喻爆点（考勤卡）+ 段尾转折 |
| S8 | 听话≠说得好 + 品牌尾卡 | 「听话」绿卡 vs 「说得好」灰卡 + 3 个风险（幻觉/过度迎合/学进有害格式）红点；RLHF/DPO 下一道工序箭头；尾卡：avatar + 关注「数解AI」+ 当期标题 +「查看公众号文章」+ 下一期预告（RLHF）+ 开放问题（预训练/SFT标注/RAG） | 但记住：SFT 解决了听话，没解决说得好。它可能编造事实，因为数据里有错误答案；可能过度迎合，因为数据偏向让用户满意；甚至学进有害格式。听话，是必要条件，不是充分条件。这就是下一道工序——RLHF——要解决的问题。关注数解AI，下一期我们拆 RLHF：怎么让模型学会'说人话'。最后问你：如果预算只够做一件事，你会投继续预训练、SFT 数据标注，还是直接做 RAG？评论区聊聊。 | 转折爆点 + 结尾预告&互动 |

## 关键数字核查（与 weixin.md 完全一致）

- 预训练成本：36氪「六小虎」至少 2 家放弃预训练；李开复「ROI 极低」；Character.AI 2024 年 8 月放弃自研
- Qwen3-8B：约 82 亿参数；官方预训练规模约 36 万亿 token
- 预训练 FLOPs ≈ 6×8.2×10⁹×36×10¹² ≈ 1.77×10²⁴；SFT（1 万条×2500 token=2500 万 token）≈ 6×8.2×10⁹×25×10⁶ ≈ 1.23×10¹⁸
- 两者相差约 144 万倍；人工成本 10,000 条×2 分钟÷60×50 元/小时 ≈ 16,667 元
- base 首 token top-10：「算法」0.570、「法」0.209（合计 78%）；post：「梯」0.813（81.3%）
- 五类指令聚合 top-1：改写 0.331→1.000、代码 0.082→0.846、情感 0.681→1.000；平均 top-1 0.820→0.916（+12%）；平均熵 0.896→0.333 bits（−63%）
- 改写指令熵 4.49 bits ≈ 22 个等概率选项
- 惩罚力度：q=0.50→1bit、0.10→3.322、0.01→6.644 bits；概率每降一个数量级惩罚翻三倍多
- 数学：交叉熵 H=−Σp log q；梯度 ∂H/∂z_j = q_j − 1[j=x*]；正确 token 负梯度上推、其他正梯度下压
- chat template：<|im_start|>user/assistant/system 角色标记；编码「什么时候该说话」
- 五、SFT 解决「听话」非「说得好」：幻觉/过度迎合/学进有害格式；下一道工序 RLHF/DPO

## 时长预估（turbo 0.19-0.20s/字符，含句间停顿；门禁 ≤4:20 不返工）

| 段 | 字符数(wc -m 预估值) | 预估时长 |
|----|------|---------|
| S1 | ~105 | ~21s |
| S2 | ~165 | ~33s |
| S3 | ~140 | ~28s |
| S4 | ~165 | ~33s |
| S5 | ~135 | ~27s |
| S6 | ~180 | ~36s |
| S7 | ~130 | ~26s |
| S8 | ~180 | ~36s |
| **合计** | **~1200** | **~240s 主体 + 拟声/停顿标签 ~10s ≈ 4:10** |

> 预估 ≈ 4:10 ≤ 4:20 门禁，直接做不返工。配音稿两版：`tts.txt` = 纯净版（字幕基准+切分 probe）；`full.txt` = 带拟声标签版（TTS 输入，`<#0.5#>` 显式停顿）。公式一律口语化（「1 点 77 乘 10 的 24 次方」「0.9 降到 0.33 bits」），符号不进 TTS。

## 实测（2026-08-15）

- TTS 整段 251.2s ≈ 4:11（tts_split 实测 VOICE_DUR 见 scenes.py）；段内无 ≥1.5s 长静音（无公式符号卡顿）
- 成品 253.1s ≈ 4:13，≤4:20 门禁

## 2026-08-15 二轮修订（用户 2 点反馈）

1. **02:58 梯度公式下标**：原单 Text 公式下标内联不美观 → 先用 sub()/sup() 手动组装（会基线错位、减号渲染成下划线，被否）→ 最终改为单 Text + Unicode 下标（ⱼ=U+2C7C 在 Noto Sans CJK SC 正常渲染为下标）：∂H/∂zⱼ = qⱼ − 1[j=x*]
2. **03:54 字幕漂移**：根因 = manim_video_build.py 字幕按字数比例分配时间，忽略真实语音停顿。两处修复：
   - build_srt() 改为优先用 full.subtitle.json 的句子级时间戳对齐每句边界（无则退回比例）
   - S8 配音稿加 <#0.5#> 把长句拆成独立 TTS 句子（听话…充分条件 / 这就是下一道工序：RLHF / 关注数解AI… 各成一句），重跑 TTS + 按新时长等比缩放全部 scenes.py at() + 重渲染
- 成品 245.5s ≈ 4:05（重跑 TTS 后时长变化）；关注数解AI 字幕与实际语音误差 ~0.2s
- 8 段 -ql 冒烟 + -qm 高清渲染，抽帧过 14 项检查（布局/越界/重叠/箭头/换页 FadeOut/音画）
- 封面：yairouter（gpt-image-2）出图 1080×1920（source ~/.bash_env 取 YAI_API_KEY；上游忽略 size 返回 941×1672，LANCZOS 放大到 1080×1920），关键内容在 3:4 安全区 y∈[350,1544]

## 制作流程

1. 配音：`scripts/minimax_tts.py` 整段生成（full.txt）+ `--subtitle` 句子级时间戳 → `scripts/tts_split.py` 按时间戳切分 8 段（克隆音色 `branding/my-voice-denoised.wav`，speed 1.15 pitch +2，speech-2.8-turbo）；生成后 `silencedetect` 扫段内长静音
2. 动画：每段一个 Manim Scene（S1..S8），`construct` 末尾 `pad_to_voice()`；动画节点按台词时间轴铺满（≥80% 配音时长）；工具函数复用预训练 scenes.py 模板（boxed/fit/sqrt_group/play_red_cross/play_scroll_unroll）
3. 构建：`scripts/manim_video_build.py` mux 配音 + 无缝拼接 + 黄色字幕烧录（MarginV=210, 75 号，整行 + 150ms 淡入）+ 品牌尾卡
4. 封面：yairouter 出 1080×1920（关键内容在中间 3:4 安全区 y∈[240,1680]）
5. 归档：`content/2026-07-29-SFT/shipinhao/`
