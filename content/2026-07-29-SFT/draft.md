---
title: "SFT微调：为什么同一个模型，突然就听懂指令了？"
series: "大模型原理"
author: "数解AI"
digest: "预训练模型什么都能说，但不知道什么时候该说、该怎么说。SFT 不灌知识，只改分布形状——用 Qwen3-8B base vs instruct 的 token 概率对比，亲眼看到'听话'是怎么发生的。"
---

同一个模型，同一个问题，两种命运。

给 Qwen3-8B 的 base 模型输入"请解释什么是梯度下降"，它续写出"**算法**，并提供一个简单的 Python 实现"——它以为你要的是代码，不是解释。

给 Qwen3-8B 的 instruct 模型同样的输入，它稳定地输出"**梯度下降**（Gradient Descent）是一种……**迭代算法**"——定义、直觉、举例，结构清晰。

权重里 99.9% 的参数是相同的。那 0.1% 的差异，到底做了什么？

答案：**它没有学会新知识，它学会了听话。**

上一篇我们讲过，预训练只做一件事——猜下一个 token。猜得越准，模型就越"懂"语言。但"懂语言"和"听指令"是两回事。SFT（Supervised Fine-Tuning，监督微调）就是从"懂语言"到"听指令"的那道工序。

下面先看一组对比实验，再用一个比喻把直觉讲清楚，最后看数学上到底发生了什么。

---

## 一、同一个模型，两种输出

我们在 AutoDL 的一张 4090 上，分别加载 Qwen3-8B 的 base 版本和 instruct 版本，用**五个不同类型的指令**做对比：解释概念、改写句子、比较异同、写代码、情感分类。

每个指令下，我们记录模型生成前 32 个 token 时每一步的 top-1 概率和信息熵，然后取平均。

### 首个 token 的概率分布

先看最直观的：第一个生成位置上，两个模型分别把多少概率押在最高分的 token 上。

以"请解释什么是梯度下降"为例：

**Base 模型**的首 token top-10：

| Token | 概率 |
|-------|------|
| 算法 | 0.570 |
| 法 | 0.209 |
| ， | 0.060 |
| ，并 | 0.060 |
| 。↵ | 0.013 |
| ？ | 0.010 |
| ？↵ | 0.010 |
| 方法 | 0.008 |
| 。↵↵ | 0.008 |
| 。 | 0.008 |

概率分散。"算法"和"法"加起来占 78%，但 base 模型不知道你要的是"解释"——它续写的是"算法，并提供一个简单的 Python 实现"，偏了。

**Instruct 模型**的首 token top-10：

| Token | 概率 |
|-------|------|
| 梯 | 0.813 |
| 当然 | 0.076 |
| ** | 0.076 |
| 好的 | 0.032 |
| " | 0.003 |
| 好 | <0.001 |
| ### | <0.001 |
| 很好 | <0.001 |
| 「 | <0.001 |

概率高度集中。"梯"一个 token 就占了 81.3%，instruct 模型直接以"梯度下降是……"开头，干净利落。

### 五个指令的聚合对比

单个 prompt 可能是巧合。五个不同类型的指令呢？

| 指令类型 | Base 首 token top-1 | Instruct 首 token top-1 |
|----------|---------------------|------------------------|
| 解释概念 | 0.902 | 0.863 |
| 改写句子 | 0.331 | 1.000 |
| 比较异同 | 0.126 | 0.432 |
| 写代码 | 0.082 | 0.846 |
| 情感分类 | 0.681 | 1.000 |

改写、代码、分类这三类指令，instruct 模型的首 token 集中度远高于 base。尤其是"情感分类"——instruct 模型以 100% 概率输出"正面"，而 base 模型还在犹豫。

再看前 32 步的**平均指标**：

| 指标 | Base | Instruct | 变化 |
|------|------|----------|------|
| 平均 top-1 概率 | 0.820 | 0.916 | +12% |
| 平均熵 (bits) | 0.896 | 0.333 | −63% |

**熵从 0.90 bits 降到 0.33 bits——不确定性减少了近三分之二。** 这就是 SFT 做的事：不是灌知识，是收窄分布。

> 实验细节：Qwen3-8B-Base 与 Qwen3-8B（post-trained），BF16 精度，greedy decoding，5 个中文指令，前 32 步 token 级指标取均值。完整数据与复现脚本见[文章仓库](https://github.com/skyscribe/content-gen)。

---

## 二、入职培训：不是重新学，是学会该说什么

预训练像大学四年。

四年里你学了微积分、线性代数、概率论、编程、经济学、心理学……你什么都知道一点。但毕业第一天走进公司，老板问你"这个季度的增长策略是什么"，你可能开始讲马尔可夫链。不是因为你不懂增长策略，而是因为你不知道**在这个场景下该说什么**。

SFT 就是入职培训。

入职培训不会教你微积分——你已经会了。它教你的是：

- 老板问策略，你就讲策略，别讲马尔可夫链
- 写邮件用"尊敬的"开头，别用"亲爱的"
- 会议纪要用 bullet point，别用散文

**知识没变，输出格式变了。**

对应到模型：

| | 预训练 | SFT |
|---|---|---|
| 目标 | 猜对下一个 token | 在给定指令下，猜对下一个 token |
| 数据 | 互联网文本（无格式约束） | 指令-回答对（有格式约束） |
| 分布形状 | 宽（什么都可能说） | 窄（该说什么就说什么） |
| 类比 | 大学四年 | 入职第一周 |

关键洞察：**预训练模型的"知识"已经足够回答问题，它只是不知道什么时候该回答、用什么格式。SFT 不增加知识量，它改变的是输出分布的形状。**

实验数据也印证了这一点。看"改写句子"这个指令——base 模型的首 token top-1 只有 0.331，熵高达 4.49 bits（接近 22 个等概率选项的不确定性）。它不是不会改写，而是不知道你要改写。instruct 模型收到指令后，首 token top-1 直接拉到 1.000——它"知道"现在该改写了。

---

## 三、数学上发生了什么：从宽分布到窄分布

### 3.1 SFT 的训练数据长什么样

SFT 的训练数据是**指令-回答对**（instruction-response pairs），格式如下：

```
<|im_start|>user
请解释什么是梯度下降<|im_end|>
<|im_start|>assistant
梯度下降是一种优化算法，用于最小化损失函数。它的核心思想是：沿着损失函数梯度相反的方向更新参数，每一步都让损失减小一点。<|im_end|>
```

注意两个关键点：

1. **`<|im_start|>user` 和 `<|im_start|>assistant`**：这是 chat template，告诉模型"这是用户在说话"和"现在轮到你回答了"。base 模型没见过这些标记，所以它不知道什么时候该接话。
2. **回答的格式是固定的**：定义 → 直觉 → 举例。SFT 数据里的回答都是这种结构，模型学会了"回答解释类问题就用这个格式"。

### 3.2 SFT 的 loss 函数

SFT 用的还是交叉熵，和预训练一模一样：

$$H(p, q) = -\sum_x p(x) \log q(x)$$

区别不在 loss，而在**数据**。

预训练数据里，"梯度下降"后面可能跟着定义、应用、论文引用、代码实现……概率分散在多种续写方式上。SFT 数据里，"请解释什么是梯度下降"后面**只有一种**回答方式。交叉熵会逼迫模型把概率集中到这种回答上。

用信息论的视角：

- 预训练后，模型对"梯度下降"后续 token 的分布熵较高——不确定性大，因为见过太多不同的续写
- SFT 后，同一位置的分布熵降低——不确定性小，因为只见过一种"正确"续写

**熵的降低 = 分布的收窄 = 从"什么都可能说"到"该说什么就说什么"。**

我们的实验量化了这个过程：五个指令的平均熵从 0.90 bits 降到 0.33 bits，降幅 63%。这不是比喻，是可测量的物理量。

### 3.3 Chat Template 的作用

Chat template 是 SFT 最容易被忽略但最关键的部分。

base 模型的输入就是纯文本续写：给它"今天天气"，它续写"很好"或"怎么样"。没有角色区分，没有对话结构。

instruct 模型的输入被 chat template 包裹：

```
<|im_start|>system
你是一个有帮助的AI助手。<|im_end|>
<|im_start|>user
请解释什么是梯度下降<|im_end|>
<|im_start|>assistant
```

这些特殊标记（`<|im_start|>`、`<|im_end|>`、`system`、`user`、`assistant`）是 SFT 阶段才学会的。它们的作用是：

- **角色标记**：告诉模型"这是用户说的"和"现在轮到你回答"
- **轮次分隔**：多轮对话中区分每一轮
- **行为约束**：system prompt 定义了模型的行为边界

没有 chat template，模型不知道什么时候该说话、以什么身份说话。**Chat template 是"入职培训"的考勤卡——不是教你知识，是告诉你"现在该你发言了"。**

---

## 四、听话不等于说得好

SFT 解决了"听话"的问题，但没解决"说得好"的问题。

一个经过 SFT 的模型会乖乖地按指令格式回答，但它可能：

- 编造事实（幻觉），因为 SFT 数据里可能有错误回答
- 过度迎合用户，因为 SFT 数据倾向于"让用户满意"的回答
- 学会了有毒的指令格式，如果训练数据里混入了有害内容

**听话是必要条件，不是充分条件。** 模型需要学会的不仅是"按指令回答"，还有"回答得对、回答得好、回答得安全"。

这就是下一道工序——RLHF/DPO——要解决的问题。它不是教模型听话（SFT 已经做了），而是教模型**说好话**：在多种可能的回答中，选择人类更偏好、更安全、更准确的那一个。

下一篇我们聊 RLHF：模型学会了听话之后，怎么让它学会"说人话"？

觉得有用就点个赞 👍、收藏 ⭐ 备用；关注「数解AI」，下一篇第一时间推给你。

---

📖 **[大模型原理合集](https://mp.weixin.qq.com/mp/appmsgalbum?__biz=MzkyMzQyODExNQ==&action=getalbum&album_id=4597831652025925632#wechat_redirect)**：BPE → 词嵌入 → 位置编码 → 注意力机制 → FFN → 归一化残差 → Transformer全景 → 预训练 → **SFT微调** → *RLHF/DPO（即将发布）*

#SFT #监督微调 #Qwen3 #大模型原理 #数解AI
