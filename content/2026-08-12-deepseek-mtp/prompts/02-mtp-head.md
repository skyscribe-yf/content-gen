---
type: framework
slug: mtp-head
backend: yairouter
model: gpt-image-2
size: "1:1"
quality: high
language: zh
output: "../02-mtp-head.png"
references: []
---

# FULL PROMPT

为微信公众号技术文章生成正方形配图，1K 分辨率。主题：DeepSeek-V3 的 MTP 侧头结构——主干照常预测「下一个词」，每个位置额外挂一个侧头（MTP 模块），输入 = 主干表示 + 真实下一个词的 embedding，拼接后过一个 Transformer block，输出预测「下下一个词」。深度 D=1。画面理性清晰，不使用人物。

## ZONES

- 左侧：一条纵向的主干管道（深炭灰粗柱，从上到下），柱顶标注「主干」。
- 中部：从主干中段分出一条细支路（金色），进入一个「拼接投影」节点（圆角方形，金色描边），节点旁有两个输入箭头：一个来自主干表示（标注「表示」），一个来自下方一排真实词中的第一个词方块（标注「真实词 embedding」）。
- 拼接投影节点向右连入一个稍小的 Transformer block 节点（深炭灰圆角方块，标注「侧头 block」），再向右接入顶部的「共享输出头」节点（宽扁圆角框，金色），输出箭头指向「下下个词」。
- 主干底部也接入同一个「共享输出头」节点，输出箭头指向「下一个词」——表现两个头共享输出头。
- 顶部：一行 token 链，第一个 token 被主头输出箭头指向，第二个 token 被侧头输出箭头指向。

## LABELS

- 节点内文字：“主干”、“拼接投影”、“侧头 block”、“共享输出头”
- 输入标注：“表示”、“真实词 embedding”
- 输出标注：“下一个词”、“下下个词”
- 顶部角落小标注：“深度 D=1”
- 其余画面不出现任何数字、英文（除 embedding）、公式、Logo。

## COLORS

- 主色：暖石墨灰（主干、block 节点）＋ 金色（侧头支路、共享输出头）
- 辅助：暗青蓝（token 链）、米白背景渐变
- 文字：深炭灰，金色框内用深色文字。

## STYLE

- 扁平矢量架构图风格，圆角节点、清晰连线。
- 视觉层级：主干是背景主柱，侧头支路是主角（金色突出）。

## ASPECT

- 正方形 1:1 构图，主干占左侧 1/4，侧头支路占中部 1/2，token 链占顶部 20%。
