---
type: comparison
slug: train-infer-mirror
backend: yairouter
model: gpt-image-2
size: "1:1"
quality: high
language: zh
output: "../05-train-infer-mirror.png"
references: []
---

# FULL PROMPT

为微信公众号技术文章生成正方形配图，1K 分辨率。主题：MTP 的训练/推理镜像——训练时语料里全是真实词，模型用真实词当条件，一次前向并行猜一串（两个交叉熵）；推理时没有真实词，侧头自己猜一串、主头一次验证一串。上下两半对称对照。画面理性清晰，不使用人物。

## ZONES

- 上半部（标注「训练」）：一条 token 链（暗青蓝实心方块，表示真实词），每个 token 上方一条竖直细箭头指向同一排两个输出位置（金色小圆点，表示一次前向两个交叉熵），旁边一个小标注「用真实词当条件」；底部一个金色小标签「两个交叉熵」。
- 下半部（标注「推理」）：同样一条 token 链，但后半段变成浅金色空心方块（表示模型自己猜的候选词），token 链上方一个侧头小节点（深炭灰）指向候选词，再一个大节点「主头一次验证」（深炭灰圆角框，内画对勾）覆盖候选词段；旁边小标注「侧头猜、主头验」。
- 上下半部之间：一条细金色分隔线，线上一个小圆点标记「镜像」。

## LABELS

- 上半部标注：“训练”、“用真实词当条件”、“两个交叉熵”
- 下半部标注：“推理”、“侧头猜、主头验”
- 分隔线标记：“镜像”
- 其余画面不出现任何英文（除 KV 类）、公式、Logo。

## COLORS

- 主色：暖石墨灰（节点、分隔线）＋ 金色（输出圆点、标签）
- 辅助：暗青蓝（真实词）、浅暖灰/浅金（候选词）、米白背景渐变
- 文字：深炭灰。

## STYLE

- 扁平矢量对比图风格，上下对称构图，圆角节点。
- 视觉层级：上下两半互为镜像，训练在上、推理在下。

## ASPECT

- 正方形 1:1 构图，上下各占 45%，分隔线占 10%。
