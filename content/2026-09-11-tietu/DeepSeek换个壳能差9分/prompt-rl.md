# DeepSeek 模型和 harness 一起训 — info graphic 生成 prompt

## 主故事线

V4.1 后训练没发明新算法，配方仍是 SFT → RL → 蒸馏。新的是 RL 同时扩两个轴：算力 × harness 数量。三层：单壳加深（DSH Minimal）、同家族多版本（Claude Code）、异质混训（OpenCode、Pi、DSH Standard/PTC）。不同壳的 checkpoint 再 merge。Agent Team 对着 DSH 协议单独训过。结论里的共同设计是将来时：现在是模型适应壳，真一起长是下一章。

## 要点卡片

**顶部标题**

- 第一行「模型和壳一起训」
- 第二行「算法没变，训的对象变了」

**上半区：配方没变（一条流程）**

- SFT → RL → 蒸馏
- 论文原话口径：没有新算法

**中部：RL 两轴（主焦点）**

- 轴一：**算力**（同一个壳里把 step 加长）
- 轴二：**harness 数量**（把不同壳丢进同一套 RL）
- 三层卡片：
  1. 单壳加深 — DSH Minimal
  2. 同家族多版本 — Claude Code
  3. 异质混训 — OpenCode / Pi / DSH
- 收束一句：不同壳的 checkpoint **merge** 回去

**下部：Agent Team**

- 不是评测时临时拼的
- 对着 DSH 的委派、邮箱、任务板训过

**底部结论条（大字）**

- 现在：模型去适应各种壳
- 下一章：模型和壳一起长
- 小字：共同设计是将来时

## 数字白名单（只允许出现以下数字，禁止任何其他数字）

无独立统计数字。允许专名里自带的数字：V4.1、Claude Code。禁止出现 9、65.5、74.2、90.6、26、2.5、60、80、100、2026 等任何分数或年份。

## 视觉要求

竖版 3:4 现代扁平信息图，白底 + 深蓝主色（#0F4C81）+ 单一强调色（橙红）用于「merge / 两轴 / 结论条」，细网格与细分隔线。顶部大标题，中部「两轴 + 三层」是画面主焦点，底部深色结论条。禁止照片、3D、渐变滥用、杂乱堆砌。禁止多图标无主焦点平铺。不要画成复杂架构图，不要公式。

## 错别字硬性禁令（最高优先级）

逐字核对以下几处，任何一个写错即整图作废：

- 「蒸馏」是「馏」字（不是「硫」）
- 「算力」是「算」字（不是「筭力」）
- 「merge」可保留英文或写「合并」，不得写成 merage / mege
- 「Claude Code」拼写正确
- 「OpenCode」是 OpenCode（不是 OpenAI Code）
- 「DSH」三个大写字母
- 「将来时」是「将」字（不是「奖来时」）
- 「适应」是「适」字（不是「是应」）

生成后请自行检查：中文字符不得有偏旁错误、替换成形近字或笔画缺失。

## 文字硬性要求

- 所有文字用简体中文；产品名可保留英文（DeepSeek、V4.1、SFT、RL、DSH、Minimal、OpenCode、Pi、Claude Code、Agent Team）
- 标题文字醒目，正文清晰可读，信息层级分明
- 画面干净，禁止出现英文长句、公式乱码、无意义装饰文字
- 除专名自带数字外，禁止任何其他数字
