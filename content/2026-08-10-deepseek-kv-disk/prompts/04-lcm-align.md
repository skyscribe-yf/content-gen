---
type: diagram
slug: lcm-align
backend: zairouter
model: gpt-image-2
size: "1:1"
quality: high
language: zh
output: "../04-lcm-align.png"
references: []
---

# FULL PROMPT

生成一张正方形中文示意图，主题：lcm 块对齐——两条压缩分支的块边界必须对齐才能按块检索。用「两个人做记号」的类比：第一个人每 3 步做记号（深青色脚印），第二个人每 5 步做记号（金色脚印），两人在第 15 步同时落脚（一个醒目的圆点）。风格克制、扁平编辑插画、不使用人物。

## ZONES

- 画面下部：一条横向路径（浅灰色长条），从 0 到 15 的刻度均匀分布（小刻度线）。
- 路径上：深青色小标记每 3 格一个，金色小标记每 5 格一个，在第 15 格处两色标记重叠成一个醒目圆点。
- 路径上方：一条虚线把第 0 格到第 15 格圈成一个大「块」，块上方标注“lcm(4,128)=128 个原始 token 一个块”。
- 右上角小标签：公式样式文字“块 = lcm(m, m')”。

## LABELS

- 只允许：块标签“lcm(4,128)=128 个原始 token 一个块”，角标“块 = lcm(m, m')”。
- 不出现任何数字公式的完整排版、品牌 Logo、网址或额外标语。

## COLORS

- 背景：温暖象牙白。
- 路径：浅灰；深青色标记与金色标记对比清晰；重叠点用深青色圆环 + 金色实心。
- 文字：深石墨黑。

## STYLE

- 精确的扁平技术编辑插画，细线、几何块面、轻微纸张纹理。
- 一眼看出「两种标记在同一位置对齐」。
