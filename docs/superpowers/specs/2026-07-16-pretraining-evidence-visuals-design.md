# 预训练文章：证据优先配图修订设计

## 目标

为 `content/2026-07-27-预训练/weixin.md` 重建会直接承载论证的配图，使图像与正文的证据边界一致。生成采用项目唯一后端 apimart.ai：封面为 21:9、1k；其余四张为 1:1、1k；图中文字全部使用中文。

## 已确认的策略

- 重做五张：封面、交叉熵解释、固定验证 PPL、固定前缀生成对比、预训练到后训练流程。
- 停用三张：能力时间线、水位/PPL 能力阶梯、声称“尚未到平台期”的 scaling-law 图。它们不再出现在正文中。
- 图中只写本文可核验的短标签。尤其不把 PPL 画成能力或意识刻度，不在图中排放长段模型生成文本。
- 所有新文件使用 `-evidence-v2` 命名，旧图片与旧提示词保留，避免覆盖不可逆地抹去历史版本。

## 五张图及位置

| 文件 | 位置 | 信息作用 |
|---|---|---|
| `00-cover-evidence-v2.png` | 开头 | 表达“预测结构逐步显现”，不标榜推理、知识或意识已浮现。 |
| `02-cross-entropy-context-v2.png` | 交叉熵等式后 | 区分单条 one-hot 标签与总体数据分布；指出低交叉熵不是理解/意识。 |
| `04-validation-ppl-evidence-v2.png` | 90 分钟指标表后 | 准确呈现 18.6/60.0/74.5/90.0 分钟及 103.90/60.54/54.82/50.73 PPL。 |
| `05-generation-evidence-v2.png` | 固定前缀文本样例后 | 显示词片段 → 故事模板与复读，明确“局部统计改善，不是稳定故事”。 |
| `07-posttraining-boundary-v2.png` | 后训练段 | 区分预训练、后训练与部署；不使用“原始大脑”“能力天花板”或固定 token 数。 |

## 验收

- 数字与 `autodl_export_20260716` 内 manifest、combined metrics 和 generation records 一致。
- 不出现“PPL 2/5/15/100 对应能力”“规模必然带来能力”“尚未到平台期”“预训练证明理解/意识”等文本或暗示。
- 生成后逐张人工检查中文、公式、数字和图文对应；若文字错误，写新版本提示词重新生成，而不覆盖或后期涂改图片。
- 插入后，正文有五张图（含封面），均与 `weixin.md` 同级且不带 `images/` 前缀。
