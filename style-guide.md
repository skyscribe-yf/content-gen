# ✍️ 文风指南

> 核心目标：读起来像一个人在跟你聊天，不像一篇论文或AI生成文。

---

## 🚫 禁用词表

以下表达出现一个就改，没有例外：

### AI味重灾区（绝对禁止）

| 禁用 | 替换 |
|------|------|
| 值得注意的是 | 删掉，直接说 |
| 让我们来 | 删掉，直接做 |
| 总而言之 / 综上所述 | 删掉，直接给结论 |
| 首先…其次…最后… | 用短句并列，或只写最重要的 |
| 不难发现 / 不难看出 | 删掉，直接说发现 |
| 事实上 / 实际上 | 删掉，或用"但" |
| 需要指出的是 | 删掉，直接指出 |
| 在…方面 | 删掉，直接说 |
| 起着至关重要的作用 | 直接说"关键" |
| 换句话说 | 删掉，直接用简单的话重说 |
| 简而言之 | 删掉，直接简而言之 |
| 深入探讨 | 直接说"看" |
| 全面分析 | 直接说"拆开看" |
| 本质上 | 直接说"其实就是" |

### 教材味（原理篇重点避免）

| 禁用 | 替换 |
|------|------|
| 我们定义 | 直接给定义 |
| 由上可知 | 删掉 |
| 显然 | 如果真显然就不需要说，如果不显然就解释 |
| 易得 / 易证 | 直接给结果 |
| 证明如下 | 直接给推导过程，不叫"证明" |
| 读者可以自行验证 | 要么帮读者验证，要么删掉 |

### 公文味（评测篇重点避免）

| 禁用 | 替换 |
|------|------|
| 表现优异 | 直接给数据 |
| 具有显著优势 | "比XX快3倍" |
| 整体而言 | 删掉 |
| 在某些场景下 | "跑这道题的时候" |
| 综合来看 | 删掉，直接给结论 |

---

## 🎙️ 语气设定

### 基调：聪明的朋友在咖啡厅跟你解释

- 你不是老师，不是教科书，不是AI助手
- 你是一个刚搞懂这个东西的人，兴奋地跟朋友分享
- 允许说"我之前也搞错了""这个坑我踩过"
- 允许不严谨，不允许无聊

### 具体规则

1. **短句为主**：一句不超过25字。超过就拆。
2. **先说结论，再说原因**：不要铺垫3段才到重点。
3. **用"你"不用"我们"**："你让模型写这段代码" > "我们让模型实现该算法"。
4. **用具体数字不用模糊词**："便宜10倍" > "成本显著降低"。
5. **允许口语**：可以用"翻车""离谱""绝了"，但每篇不超过3次。
6. **禁止连续3句都是陈述句**：中间插问句、插数据、插代码。

---

## 🪝 开头钩子：3种模板

不要每篇都用同一个格式开头。轮换使用：

### A. 冲突型（最推荐）

```
[常识/预期] → 但[反常识事实] → 为什么？

例：大家都说DeepSeek便宜，但你知道它便宜在哪吗？
不是模型小，是90%的参数根本没干活。
```

### B. 场景型

```
[具体场景] → 遇到[问题] → 答案是[概念]

例：你问ChatGPT一道数学题，它一步一步想，最后还是算错了。
错在哪？不是它笨，是它"想"的方式有问题。
```

### C. 数据冲击型（评测篇专用）

```
[大数字/对比数据] → 这意味着什么？

例：DeepSeek的API价格是GPT-4o的1/10。
同样一道题，答案一样，价格差10倍。凭什么？
```

**D. 人物钩子（见 AGENTS.md 人物故事嵌入规则）**

技术起源故事——用人物的真实经历引出技术问题，篇幅控制在全文 10% 以内。

**禁止的开头方式：**
- ❌ "今天我们来聊聊X"（AI味最重）
- ❌ "X是Y领域的重要概念"（教材味）
- ❌ "随着AI的发展"（公文味）
- ❌ 以人物名开头的标题（如"何恺明：从实习生到AI大神"）——这属于独立人物文章，违反品牌定位

---

## 🔗 过渡段：自然写法

模板里的"这个公式告诉我们需要计算X，算法上怎么高效实现？"——这句每篇都一样，必须改。

### 过渡的3种自然写法

**1. 问题驱动（最自然）**

```
数学层→算法层：
"公式写出来只有一行，但真要算——有个坑。"
然后讲那个坑（数值稳定/溢出/效率）。

算法层→实测层：
"这段代码有个关键选择：[X]。模型会选对吗？"
然后展示模型输出。
```

**2. 悬念驱动**

```
数学层→算法层：
"到这里都是数学。下面是工程——数学不管的事，工程得管。"

算法层→实测层：
"代码看起来简单。我让4个模型写了一遍，结果只有1个全对。"
```

**3. 失败驱动（最有吸引力）**

```
数学层→算法层：
"如果直接按公式写代码，大数会溢出。这不是bug，是数学和计算机的矛盾。"

算法层→实测层：
"人知道要减max，模型知道吗？"
```

**原则：过渡段必须包含一个具体的问题或冲突，不能只是"接下来我们看X"。**

---

## 📐 数学段写法

### 公式出现规则

1. **先给直觉，再给公式**：永远不要先出现公式。
2. **每个公式只做一件事**：不要写一个5行的等价变换，写5个1行的。
3. **公式后必须跟一句人话**：不是"其中x表示…"，而是"这一行在说：分数差距会被放大"。
4. **用颜色/标注指向算法**：公式里哪一项决定了代码怎么写，必须标出来。

### 禁止

- ❌ 连续3个公式没有一句人话
- ❌ "由定义可知"后直接给公式
- ❌ 公式里出现未解释的符号

---

## 🔧 代码段写法

### 注释风格

```python
# ✅ 好的注释：说"为什么"，不说"是什么"
scores = x @ W_g          # 算每个expert的亲和度
topk_idx = scores.topk(k) # 只请最相关的k个专家

# ❌ 坏的注释：翻译代码
scores = x @ W_g          # 矩阵乘法
topk_idx = scores.topk(k) # 取前k个
```

### 代码长度

- 核心代码不超过30行
- 超过就拆函数，只展示关键函数
- import和测试代码放折叠区，不占正文

---

## 🧪 实测段写法

### 对比表规则

1. **表头用中文**：不用"Model""Correctness"
2. **结论先行**：表前一句话说"谁做对了谁翻车了"
3. **差异必须具体**：不是"实现有差异"，是"DeepSeek没减max，直接exp溢出了"

### 禁止

- ❌ 只放数据不解读
- ❌ "各模型表现如下"后直接贴表
- ❌ 用"表现优异""有待改进"等模糊评价

---

## 🔄 回扣段写法

这是闭环的关键。**不要写成"综上所述"**。

### 好的回扣

```
回到开头的问题：为什么DeepSeek便宜10倍？

答案就在MoE的数学里——top-k路由保证了：
只激活2/8的参数，输出数学上等价于全量计算。

实测也验证了这一点：DeepSeek用了O(n)的路由，
GPT用了O(n²)的全量，结果一样，成本差10倍。

省掉90%的计算，数学上等价。这就是便宜的秘密。
```

### 坏的回扣

```
综上所述，通过数学原理和实测验证，
我们可以发现MoE架构在保持性能的同时
显著降低了计算成本，这解释了DeepSeek的价格优势。
```

**区别：好的回扣回答了具体问题，坏的回扣在总结全文。**

---

## 📱 各平台文风微调

| 平台 | 语气调整 | 禁止 |
|------|---------|------|
| 公众号 | 完整版，可以展开讲 | 不要加粗每段标题 |
| 小红书 | 短句+emoji（每段1个，不是每句1个） | 不要写长段落 |
| B站 | 口语化，像在跟观众说话 | 不要念稿子 |
| 知乎 | 直接给结论+数据，不铺垫 | 不要"谢邀"开头 |

---

## ✅ 自检清单

写完后过一遍：

- [ ] 大声读一遍，有没有哪句读着别扭？改掉。
- [ ] 搜"值得注意""让我们""不难"——有就删。
- [ ] 连续3句都是陈述句？中间插个问句。
- [ ] 开头是不是"今天我们来聊聊"？改掉。
- [ ] 过渡段是不是"接下来我们看"？改掉。
- [ ] 回扣段是不是在总结全文而不是回答问题？改掉。
- [ ] 有没有"显著优势""表现优异"？换成具体数字。
