# ✍️ 文风指南

> 核心目标：读起来像一个人在跟你聊天，不像一篇论文或AI生成文。

---

## 🚫 禁用词表

以下表达出现一个就改，没有例外：

### AI味重灾区（绝对禁止）

| 禁用 | 替换 |
|------|------|
| 值得注意的是 | 删掉，直接说 |
| 让我们来 | 删掉，直接做 |
| 总而言之 / 综上所述 | 删掉，直接给结论 |
| 首先…其次…最后… | 用短句并列，或只写最重要的 |
| 不难发现 / 不难看出 | 删掉，直接说发现 |
| 事实上 / 实际上 | 删掉，或用"但" |
| 需要指出的是 | 删掉，直接指出 |
| 在…方面 | 删掉，直接说 |
| 起着至关重要的作用 | 直接说"关键" |
| 换句话说 | 删掉，直接用简单的话重说 |
| 简而言之 | 删掉，直接简而言之 |
| 深入探讨 | 直接说"看" |
| 全面分析 | 直接说"拆开看" |
| 本质上 | 直接说"其实就是" |

### 教材味（原理篇重点避免）

| 禁用 | 替换 |
|------|------|
| 我们定义 | 直接给定义 |
| 由上可知 | 删掉 |
| 显然 | 如果真显然就不需要说，如果不显然就解释 |
| 易得 / 易证 | 直接给结果 |
| 证明如下 | 直接给推导过程，不叫"证明" |
| 读者可以自行验证 | 要么帮读者验证，要么删掉 |

### 公文味（评测篇重点避免）

| 禁用 | 替换 |
|------|------|
| 表现优异 | 直接给数据 |
| 具有显著优势 | "比XX快3倍" |
| 整体而言 | 删掉 |
| 在某些场景下 | "跑这道题的时候" |
| 综合来看 | 删掉，直接给结论 |

---

## 🎙️ 语气设定

### 基调：聪明的朋友在咖啡厅跟你解释

- 你不是老师，不是教科书，不是AI助手
- 你是一个刚搞懂这个东西的人，兴奋地跟朋友分享
- 允许说"我之前也搞错了""这个坑我踩过"
- 允许不严谨，不允许无聊

### 具体规则

1. **短句为主**：一句不超过25字。超过就拆。
2. **先说结论，再说原因**：不要铺垫3段才到重点。
3. **用"你"不用"我们"**："你让模型写这段代码" > "我们让模型实现该算法"。
4. **用具体数字不用模糊词**："便宜10倍" > "成本显著降低"。
5. **允许口语**：可以用"翻车""离谱""绝了"，但每篇不超过3次。
6. **禁止连续3句都是陈述句**：中间插问句、插数据、插代码。

### 人味儿八法

对话语气做对了 ≠ 有个人温度。以下八个技法解决"读起来像好教科书，不像活人"的问题。**每篇至少用到 4 个，覆盖前四法中的至少 2 个。**

人味儿的本质是——读者读完，能感觉到这篇文章**背后站着一个活人**，而不是一个知识搬运工具。活人会翻车、会生气、会承认自己不懂、会说"这个我也没想到"。

---

#### ① 埋"我"进去

把"这件事是这样"改成"我第一次看到的时候…"。让读者跟你一起经历那几秒钟的震撼或困惑。

每篇用 2-3 次，分散在不同段落，不集中在开头。放在最反直觉、最有冲击力的点上。

```markdown
❌ 这 9.27 亿个数字不是人填的。它们一开始是随机的，然后在训练中被反向传播一步步调整。

✅ 我第一次看到 embedding 这 9.27 亿个数字的时候，脑子里只有一个问题：谁填的？
   答案差点让我把键盘摔了——没人填。全是随机初始化，训练时反向传播一点点拧出来的。
   听起来像作弊，但它确实管用。
```

> 关键：不讲知识本身，讲你**发现**这个知识时的反应。知识是冷的，反应是热的。

#### ② 先卡住，再走出来

在讲最瓶颈的概念之前，先承认自己也被卡住过——而且说实话，卡得很难看。读者会感到你跟他们站在一起，不是居高临下。

每篇用 1-2 次，放在整篇最难的概念上。卡住的时间越长、经历越具体，越可信。

```markdown
❌ 看到 QKᵀ，很容易以为注意力是"矩阵层面的操作"。本质是 token i 拿自己的 Q 和所有 K 算匹配分。

✅ 我盯着 QKᵀ 看了十分钟，满脑子都是"两个矩阵乘出来是什么鬼"。
   后来发现别管整个矩阵——只看一行，它就是 token i 在和所有 token 算匹配分。
```

加强版（更具体）：

```markdown
✅ 老实说，矩阵吸收花了我三天才想通。三天里我翻了三篇博客、看了原论文附录、
   还用草稿纸推了两遍——每次推到一半就卡在同一个地方。
   直到我把结合律写在一行里。看懂的时候我有点生气——就这么简单？
```

> "三天"比"十分钟"更有分量。"卡在同一个地方"比"一开始没懂"更具体。

#### ③ 比喻讲成故事

不要只给类比（"X 就像 Y"），让比喻里出现一个**做蠢事的人**，并且给他一个**不断升级的困境**。最好的比喻有三个要素：角色、动作、后果。

每篇用 1 次，放在最核心的概念上。不要把每个比喻都升级——只升级最重要的那一个。

```markdown
❌ 嵌入矩阵像中药柜：拉开抽屉倒药是前向传播，调整配方是反向传播。

✅ 想象学徒第一天去中药铺。师父给了配方：当归、川芎、白芷。
   他想偷懒——川芎和当归颜色差不多，抓一把得了。

   错了。机器不是学徒，它不偷懒。
   它拉开抽屉倒粉、算配方效果，倒完立刻偷偷把每个抽屉里的粉末配方调 0.0001 毫米。
   几百亿次后，当归和川芎的粉末几乎一模一样——
   不是谁教的，是它们老被开在同一张方子里。
```

升级版（困境递进）：

```markdown
✅ 想象你是一个实习生，老板让你做会议记录。每有人说一句话你就写一张便利贴。
   一小时后，墙上贴了 200 张。老板问："第一分钟，老张说了什么？"
   你从 200 张里翻。找到了。但找的过程比会本身还长。

   更要命的是——隔壁会议室也在开会，每间会议室都有自己的一面墙。
   30 层楼，每层十几间会议室。你是唯一的实习生。

   好了，现在告诉我，你要怎么办？
```

> 升级版不只是"实习生做记录"——它把困境**推到了极限**（30 层楼、唯一实习生），让读者自己先想几秒钟，然后再给答案。

#### ④ 抽离闲笔

技术结论之后，偶尔加一句**不带教学目的的私人判断**。不是科普，是你自己的嘴唇在动。

每篇用 2-3 次，放在技术段落的最后一句话。多了一点也不油——读者的耳朵需要偶尔换节奏。

```markdown
❌ 残差连接和 LayerNorm 补上了深层网络的短板，所以 T=1 成了训练默认值。

✅ 所以 T=1 成了默认值，不是因为数学多漂亮，是因为残差连接和 LayerNorm 替它兜了底。
   有时候工程就是这样——优雅方案搞不定，丑方案配一个好搭档，居然就稳了。
```

进阶：不只讲工程感慨，也可以讲阅读体验、审美判断、甚至自嘲。

```markdown
✅ 看懂这一行的时候我有点生气——就这么简单？我卡了三天，最后是一行结合律。

✅ 说实话，我越来越觉得，好架构师的直觉不是"把东西做复杂"，而是"把耦合拆开"。

✅ 这大概就是做系统的人最上瘾的时刻：你不是在优化一个数字，你是在改写一个问题的定义。
```

> 核心：闲笔不能有"教"的意图。它只是你站在文章旁边，自己嘀咕了一句。

---

#### ⑤ 自嘲

不把自己架在"专家"的位置上。偶尔承认自己曾经犯过一个很蠢的错误、或者长期搞错了一件事。自嘲的力度越大，读者越信任你。

每篇用 1 次就够了，放在最容易让读者"自以为懂了"的知识点上。

```markdown
❌ MLA 与 GQA 是不同的技术路线。

✅ 我承认——我刚看到 DeepSeek 论文的时候，以为 MLA 就是 GQA 换了个皮肤。
   毕竟"压缩 KV cache"这个说法，GQA 也能沾上边。看了 Table 9 的数据才闭嘴。
```

> "以为 X 就是 Y 换了个皮肤"——大多数读者其实也这么想，你先替他们说了出来。

#### ⑥ "读论文"时刻

给读者看你是怎么从原始资料里挖出关键信息的——翻 config.json、对比 Table 9、找审稿人会怎么质疑。把研究过程本身变成叙事。

每篇用 1 次，放在涉及模型参数或论文数据的段落。

```markdown
❌ DeepSeek-V2 报告中 Table 9 的数据显示……

✅ 我第一次翻到 Table 9 的那两行数字——110.6K 变成 15.6K——
   心里想的不是"哇，好厉害"。我想的是：
   如果我是审稿人，我会要求他们解释"为什么压缩这么多还能打平"。
   论文确实做了这件事，用 JL 引理给了理论支撑。
```

#### ⑦ 跨篇 callback（带个人记忆）

引用前文时，不只贴链接，加一句你对那篇的个人记忆。让系列读者感到"这个作者记得自己写过什么"。

每篇用 1-2 次。

**禁止生硬的回读句式**（每篇最多 1 次回读，且必须用推荐句式）：

```markdown
❌ 可回读《DeepSeek便宜30倍的秘密：MoE混合专家入门》。
❌ 真实国产模型如何用路由组织多个 FFN，可回读前文。
❌ 关于 Q、K、V 的详细讲解，请参阅前文《注意力机制是什么？》。
❌ 想了解更多，请回看前文《残差连接》。
```

**推荐的回读句式**：

```markdown
✅ 如果你对"多个专家 FFN 怎样被路由器调度"感兴趣，我前面这篇聊得更详细：[《DeepSeek便宜30倍的秘密》](URL)。
✅ 想知道这条路为什么关键，建议回头看看我前面这篇：[残差连接：为什么 56 层比 20 层还差](URL)。
✅ 你在[词嵌入那篇](URL)看到的中药柜，是"用出来的"语义空间。
   MLA 的压缩投影，也是"用出来的"摘要——不是人设计的，是梯度逼出来的。
```

**原则**：回读是给读者一个"你可能还想看"的建议，不是布置作业。语气要像朋友推荐，不是目录索引。

#### ⑧ 承认不知道

遇到理论不够完备、实验不够充分、或者你自己也没完全搞懂的地方——直说。比假装全懂更可信。

每篇 1 次，放在理论推导或数据解读的尾部。

```markdown
❌ JL 引理从理论上保证了低维投影可以保留距离关系。

✅ 说句实话：JL 引理给的是存在性证明——"存在一个投影能保距离"。
   它没告诉你这个投影长什么样，也没说从数据里学出来的投影一定满足它的误差界。
   MLA 的实际效果是靠实验验证的，不是靠 JL 引理推导出来的。
   我觉得论文作者引 JL 更多是给读者一个"这事不违反数学直觉"的底气，而不是严格的理论保证。
```

> "我觉得"——你亮出了自己的判断，而且承认了理论的边界。

#### ⑨ 拉读者算一笔账

突然停下来，用"你可能会想…"把读者拉进对话。然后从公开数据源（config.json、论文表格、HuggingFace model card）掏真实数字，**现场算给读者看**。算出一个离谱的结果，用一个反差巨大的比较收尾。

核心不是科普"这个模型有多大"，而是让读者**和你一起经历"卧槽这么大"的那两秒钟**。

每篇 1 次，放在读者最可能产生"为什么不…"疑问的地方。数据必须可溯源。

```markdown
❌ GLM-5.2 是一个 753B 参数的 MoE 模型，无法在消费级显卡上运行。

✅ 说到这个——你可能会想：7B 太小了，换个大的不行吗？GLM-5.2 能不能装进 4090？
   来，算一笔账。GLM-5.2 总参数 753B，从 HuggingFace config.json 掏真实数字：
   78 层、KV 压缩到 512 维。算下来，每 token KV cache 约 88KB。
   128K 上下文一开——光历史记录就 10.7GB。模型还没装呢。
   模型本身呢？753B 参数，压到 4-bit 也要 ~377GB。
   一张 4090 24GB——显卡直接笑出声。你就是买十六张 4090 串起来，刚够装模型权重，KV cache 另算。
```

> 三步：① 替读者说出疑惑（"换个大的不行吗？"）② 拉真实数字现场算 ③ 用极端比较收尾（"十六张 4090 串起来"）。读者不是被教育了——是被带着一起算了一遍。

---

### 用量指南

| 技法 | 最低 | 推荐 | 适用位置 |
|------|------|------|---------|
| ① 埋"我" | 2 次 | 3 次 | 最反直觉的知识点上 |
| ② 先卡住 | 1 次 | 2 次 | 整篇最难的概念 |
| ③ 比喻讲故事 | 1 次 | 1 次 | 核心概念，只升级一个 |
| ④ 抽离闲笔 | 2 次 | 3 次 | 任意技术段尾 |
| ⑤ 自嘲 | 0 | 1 次 | 容易"自以为懂了"的点 |
| ⑥ 读论文时刻 | 0 | 1 次 | 涉及论文数据处 |
| ⑦ 跨篇 callback | 1 次 | 2 次 | 引用前文处 |
| ⑧ 承认不知道 | 0 | 1 次 | 理论边界处 |
| ⑨ 拉读者算账 | 0 | 1 次 | 读者可能产生"为什么不…"的地方 |

**底线**：前四法用到 ≥ 4 次（覆盖至少 2 个不同技法） + 后五法用到 ≥ 3 次（任意组合）= **每篇至少 7 处人味儿注入**。

### 人味儿自检

- [ ] 至少有 2 处"我"——不是"我们"，不是"我跑了一次实验"，是"我第一次看到/我当时以为/我被坑过"？
- [ ] 至少有 1 次我说了一个具体的翻车经历（时间长度、翻了几篇博客、卡在什么地方）？
- [ ] 比喻里出现了一个有名字的角色（学徒/实习生/新手）并且他的困境在升级？
- [ ] 至少有 2 句闲笔——删掉它们，科普准确性不变，但文章温度明显下降？
- [ ] 至少 1 处我承认了自己曾经搞错（自嘲）或承认了理论的边界（承认不知道）？
- [ ] 至少 1 处 callback 前文时带了个人记忆，而不只是贴链接？
- [ ] 读完文章，能感觉到这个人有自己的审美和判断（"我觉得这很扯/这很漂亮"），而不仅仅是"这很重要"？
- [ ] 至少 1 处我拉读者现场算了一笔账（"你可能会想…来算一笔账…"），数据来自公开 config.json/论文表格？
- [ ] 如果删除所有第一人称句，这篇文章还剩任何温度吗？——如果没有，再加。

---

## 🪝 开头钩子：3种模板

不要每篇都用同一个格式开头。轮换使用：

### A. 冲突型（最推荐）

```
[常识/预期] → 但[反常识事实] → 为什么？

例：大家都说DeepSeek便宜，但你知道它便宜在哪吗？
不是模型小，是90%的参数根本没干活。
```

### B. 场景型

```
[具体场景] → 遇到[问题] → 答案是[概念]

例：你问ChatGPT一道数学题，它一步一步想，最后还是算错了。
错在哪？不是它笨，是它"想"的方式有问题。
```

### C. 数据冲击型（评测篇专用）

```
[大数字/对比数据] → 这意味着什么？

例：DeepSeek的API价格是GPT-4o的1/10。
同样一道题，答案一样，价格差10倍。凭什么？
```

**D. 人物钩子（见 AGENTS.md 人物故事嵌入规则）**

技术起源故事——用人物的真实经历引出技术问题，篇幅控制在全文 10% 以内。

**禁止的开头方式：**
- ❌ "今天我们来聊聊X"（AI味最重）
- ❌ "X是Y领域的重要概念"（教材味）
- ❌ "随着AI的发展"（公文味）
- ❌ 以人物名开头的标题（如"何恺明：从实习生到AI大神"）——这属于独立人物文章，违反品牌定位

---

## 🔗 过渡段：自然写法

模板里的"这个公式告诉我们需要计算X，算法上怎么高效实现？"——这句每篇都一样，必须改。

### 过渡的3种自然写法

**1. 问题驱动（最自然）**

```
数学层→算法层：
"公式写出来只有一行，但真要算——有个坑。"
然后讲那个坑（数值稳定/溢出/效率）。

算法层→实测层：
"这段代码有个关键选择：[X]。模型会选对吗？"
然后展示模型输出。
```

**2. 悬念驱动**

```
数学层→算法层：
"到这里都是数学。下面是工程——数学不管的事，工程得管。"

算法层→实测层：
"代码看起来简单。我让4个模型写了一遍，结果只有1个全对。"
```

**3. 失败驱动（最有吸引力）**

```
数学层→算法层：
"如果直接按公式写代码，大数会溢出。这不是bug，是数学和计算机的矛盾。"

算法层→实测层：
"人知道要减max，模型知道吗？"
```

**原则：过渡段必须包含一个具体的问题或冲突，不能只是"接下来我们看X"。**

---

## 📐 数学段写法

### 公式出现规则

1. **先给直觉，再给公式**：永远不要先出现公式。
2. **每个公式只做一件事**：不要写一个5行的等价变换，写5个1行的。
3. **公式后必须跟一句人话**：不是"其中x表示…"，而是"这一行在说：分数差距会被放大"。
4. **用颜色/标注指向算法**：公式里哪一项决定了代码怎么写，必须标出来。

### 禁止

- ❌ 连续3个公式没有一句人话
- ❌ "由定义可知"后直接给公式
- ❌ 公式里出现未解释的符号

---

## 🔧 代码段写法

### 注释风格

```python
# ✅ 好的注释：说"为什么"，不说"是什么"
scores = x @ W_g          # 算每个expert的亲和度
topk_idx = scores.topk(k) # 只请最相关的k个专家

# ❌ 坏的注释：翻译代码
scores = x @ W_g          # 矩阵乘法
topk_idx = scores.topk(k) # 取前k个
```

### 代码长度

- 核心代码不超过30行
- 超过就拆函数，只展示关键函数
- import和测试代码放折叠区，不占正文

---

## 🧪 实测段写法

### 对比表规则

1. **表头用中文**：不用"Model""Correctness"
2. **结论先行**：表前一句话说"谁做对了谁翻车了"
3. **差异必须具体**：不是"实现有差异"，是"DeepSeek没减max，直接exp溢出了"

### 禁止

- ❌ 只放数据不解读
- ❌ "各模型表现如下"后直接贴表
- ❌ 用"表现优异""有待改进"等模糊评价

---

## 🔄 回扣段写法

这是闭环的关键。**不要写成"综上所述"**。

### 好的回扣

```
回到开头的问题：为什么DeepSeek便宜10倍？

答案就在MoE的数学里——top-k路由保证了：
只激活2/8的参数，输出数学上等价于全量计算。

实测也验证了这一点：DeepSeek用了O(n)的路由，
GPT用了O(n²)的全量，结果一样，成本差10倍。

省掉90%的计算，数学上等价。这就是便宜的秘密。
```

### 坏的回扣

```
综上所述，通过数学原理和实测验证，
我们可以发现MoE架构在保持性能的同时
显著降低了计算成本，这解释了DeepSeek的价格优势。
```

**区别：好的回扣回答了具体问题，坏的回扣在总结全文。**

---

## 📱 各平台文风微调

| 平台 | 语气调整 | 禁止 |
|------|---------|------|
| 公众号 | 完整版，可以展开讲 | 不要加粗每段标题 |
| 小红书 | 短句+emoji（每段1个，不是每句1个） | 不要写长段落 |
| B站 | 口语化，像在跟观众说话 | 不要念稿子 |
| 知乎 | 直接给结论+数据，不铺垫 | 不要"谢邀"开头 |

---

## ✅ 自检清单

写完后过一遍：

- [ ] 大声读一遍，有没有哪句读着别扭？改掉。
- [ ] 搜"值得注意""让我们""不难"——有就删。
- [ ] 连续3句都是陈述句？中间插个问句。
- [ ] 开头是不是"今天我们来聊聊"？改掉。
- [ ] 过渡段是不是"接下来我们看"？改掉。
- [ ] 回扣段是不是在总结全文而不是回答问题？改掉。
- [ ] 有没有"显著优势""表现优异"？换成具体数字。
